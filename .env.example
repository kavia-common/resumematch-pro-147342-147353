# Workspace-level .env template notes
# Backend:
#   Copy backend/.env.example to backend/.env and set JWT_SECRET and CORS_ALLOWED_ORIGINS as needed.
# Frontend:
#   Copy react_frontend/.env.example to react_frontend/.env and set REACT_APP_API_BASE to your backend URL.
# Database:
#   This project uses MongoDB (MONGODB_URL, MONGODB_DB). The database container advertised as MySQL is not used by this backend.
