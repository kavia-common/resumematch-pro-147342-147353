# Backend (Spring Boot) - ResumeMatch Pro
# Copy this file to .env and adjust values as needed for your environment.

# MongoDB - REQUIRED for persistence. Backend expects Mongo, not MySQL.
# If you don't have MongoDB running locally, you can point MONGODB_URL to a remote instance.
MONGODB_URL=mongodb://localhost:27017
MONGODB_DB=resumematch_pro

# JWT - REQUIRED. Use a long random string (>=32 chars for HS256/HMAC).
# Without this, the backend will fail to start by design.
JWT_SECRET=change-me-please-with-a-long-random-secret-at-least-32-characters
JWT_EXP_MINUTES=120

# File storage for uploads (backend will create if missing)
FILE_STORAGE_DIR=./storage

# CORS - Allowed origins (comma-separated). Include your frontend preview URL and localhost:3000 for local dev.
# Example (local dev): http://localhost:3000
# Example (Kavia preview): https://vscode-internal-20999-beta.beta01.cloud.kavia.ai:3000
CORS_ALLOWED_ORIGINS=http://localhost:3000
