# Backend Environment Variables for ResumeMatch Pro

# MongoDB connection
# If not set, backend defaults to mongodb://localhost:27017
MONGODB_URL=mongodb://localhost:27017
# If not set, backend defaults to resumematch_pro
MONGODB_DB=resumematch_pro

# JWT configuration
# REQUIRED: No default. Set to a strong random value (32+ chars recommended).
JWT_SECRET=change-this-to-a-long-random-string
# Optional, defaults to 120 minutes
JWT_EXP_MINUTES=120

# File storage base directory (optional; defaults to ./storage relative to backend)
FILE_STORAGE_DIR=./storage

# CORS configuration (optional; defaults to http://localhost:3000)
CORS_ALLOWED_ORIGINS=http://localhost:3000
